# CELL064 Degradation Model — Key Findings (2026-09-20)

**Scope:** three findings from the CELL064 (GMFEB23S NMC622/Si-Gr pouch cell)
PyBaMM degradation-model investigation, covering (1) why DMA-consistent
LLI/LAM scaling matters for RPT-shape fidelity, (2) the post-knee
porosity-gated SEI-induced isolation mechanism that drives LAM, and (3) why
the Beginning-of-Life (BoL) solid-diffusion parameterization (Si particle
radius and diffusivity) controls whether the model's End-of-Life (EoL)
stoichiometric range matches reality.

All model/code references are to
`packages/pybamm/src/pybamm/models/submodels/` (core PyBaMM) and
`packages/pybamm/si_gr_expansion_precursor/pouch_cell_064/degradation_test_matrix/cell064_degradation_fit.py`
(the CELL064-specific fit driver), current as of this session.

**Note: "old baseline" means something different in Finding 1 vs. Finding 3**,
since each isolates a different mechanism:

- **Finding 1's old baseline** = the canonical, pre-session CELL064 fit
  (`cell064_degradation_fit_result.png` / `cell064_expansion_fit_result.png`
  / `cell064_overall_summary_result.png`, dated 2026-09-14) — the actual run
  that motivated this whole investigation into increasing Si LAM in the
  first place. Its LAM_Si reaches only ~28% by EFC~230, well below the real
  DMA-derived values (58% at EFC~172, 80% at EFC~197). Its exact parameter
  set turned out to be reconstructable despite predating raw-curve saving
  and having no saved snapshot (an untracked, iteratively-tuned script) --
  the README documents "item 19 final: run07" as six specific parameters
  (`C64_K_SEI_MULT=1.0e-3`, `C64_NEG_POROSITY_FLOOR=0.023`,
  `C64_EXPONENT_MAX_SEI=100`, `C64_LAM_PROP_MULT=0.6`,
  `C64_SI_BETA_LAM_SEI=7e-6`, BoL porosity 0.25), and all six are still the
  script's own literal current defaults, so running with zero overrides
  reconstructs it directly. One stability fix was needed: the documented
  floor (0.023) now hits a solver failure (`IDA_BAD_K`) shortly post-knee --
  something in the shared submodel code has shifted the numerically-safe
  margin since 2026-09-14 (this session's own later work independently
  needed 0.035 for similar reasons). Raising the floor to 0.028 (plus
  `MAX_CYCLES` 250->350, also documented in the README as needed to bring
  RPT4/RPT5 back into range) reproduces the documented knee EFC of 107.7
  exactly and completes with a natural SoH-floor stop rather than crashing.
- **Finding 3's old baseline** = a specific, exactly-reproducible config
  used throughout this session's early diffusivity diagnostics: Si LAM WITH
  the porosity-gated isolation mechanism turned ON (`SI_BETA_LAM_ISO=1.0`,
  redirect-to-LAM active, BoL porosity 0.13, `SI_K_SEI_MULT=6e-4`/
  `GR_K_SEI_MULT=1.25e-5`, pore-buffering F0=0.7/width=0.05), but Si
  particle radius/diffusivity/exchange-current all still at their
  literature (never-fitted) values. Finding 3 is specifically about the
  diffusion-bottleneck effect that only shows up *once* LAM is already
  severe enough to concentrate current density on the remaining active Si.

"New baseline" (used in both findings) = the config adopted at the end of
this session: Si particle radius x0.2, diffusivity x3, BoL porosity 0.17,
`SI_K_SEI_MULT=3e-4`/`GR_K_SEI_MULT=6e-6`, pore-buffering F0=0.7/width=0.03
(isolation mechanism ON, same as Finding 3's old baseline, so in Finding 3
ONLY the diffusion-related parameters differ between old and new).

---

## Finding 1: DMA-consistent LLI/LAM scaling is critical for RPT4/RPT5 shape fidelity

The model's total capacity fade at any point in life is the sum of two
independent degradation modes: **LLI** (lithium inventory lost to SEI/side
reactions) and **LAM** (active material lost, mainly Si via the porosity-
isolation mechanism, Finding 2). A given total SoH can be reached by very
different LLI/LAM *splits* — and the split matters far beyond the aggregate
SoH number, because LLI and LAM shift the discharge curve's *shape*
differently:

- **LLI** shifts the whole anode/cathode stoichiometry window relative to
  each other (a lithium-inventory problem) — it mostly shifts the discharge
  curve along the voltage axis and shortens it roughly proportionally.
- **LAM** (specifically Si LAM) removes an entire *phase's* contribution to
  the composite anode's OCP and capacity — it changes the discharge curve's
  *shape*, not just its length, because the remaining electrode is a
  different (more graphite-dominated) composite than before.

The **old baseline** had proportionally more LLI than Si LAM relative to
what the real CELL064 DMA/eSOH fits show (`CELL064_LAM_derived.csv`:
real LAM_Si reaches 58.5% by RPT4/EFC~171 and 79.6% by RPT5/EFC~197 — a
large, dominant contribution). With too little LAM_Si and too much LLI, the
old baseline's RPT4/RPT5 discharge curves end up shaped more like a
lithium-inventory-limited curve than the real, LAM-Si-dominated one —
visible directly as a shape mismatch (not just a magnitude mismatch) in
both the raw V(Q) curve and its dV/dQ signature (dV/dQ is far more
shape-sensitive than V(Q) itself, since it amplifies curvature differences
that are hard to see by eye on the raw discharge curve).

The **new baseline** re-tunes `SI_K_SEI_MULT`/`GR_K_SEI_MULT`/BoL porosity
together (see Findings 2 and 3) so the model's LLI/LAM_Si split tracks the
real DMA-derived split much more closely across life, not just the
aggregate SoH-vs-EFC curve.

**Update:** the old baseline's exact config turned out to be reconstructable
after all (see the note above) — `build_true_old_baseline.py` reproduces it
by running with zero env var overrides (all six parameters the README
documents for "item 19 final: run07" are still the file's own literal
defaults), needing one debugged stability fix (`NEG_POROSITY_FLOOR` 0.023 ->
0.028 and `MAX_CYCLES` 250 -> 350, since the exact original floor now hits a
solver failure just past the knee — something in the shared submodel code
has shifted the numerically-safe margin since Sep 14; the fix reproduces
the documented knee EFC of 107.7 exactly and reaches a natural SoH-floor
stop, not a crash). This makes a genuine 3-way (real / true old / new)
comparison possible for the RPT4/RPT5 curves.

**Plots:**
- `cell064_overall_summary_result_true_old_baseline.png` (old baseline,
  stability-fixed reconstruction of the 2026-09-14 config) vs.
  `cell064_overall_summary_result_pbuf2_f0p7_w0p03.png` (new baseline) —
  each is the full summary panel (SoH, LAM+LLI combined, RPT voltage,
  reversible expansion, expansion scale k) run against real data
  independently; compare the "LAM by phase + LLI" panel between the two
  directly: the old baseline's Si curve visibly undershoots the real DMA
  points (LLI, dashed, overshoots instead) while the new baseline's LAM_Si
  tracks the real points much more closely. Both now also show the real
  discharge curve overlaid (dashed) on the "Discharge voltage vs. RPT"
  panel — the old baseline previously lacked this (that panel's real
  overlay is a feature added after the original 2026-09-14 run, so the
  as-originally-generated file predates it; the stability-fixed
  reconstruction above uses current code and picks it up automatically).
- `final_rpt45_voltage_3way.png` — RPT4 and RPT5 discharge V(Q): real vs.
  true old baseline vs. new baseline, all three genuinely overlaid. The new
  baseline's curve *shape* (particularly the mid-depth knee/bend around
  60-90% of the discharge) tracks the real curve's curvature noticeably
  better than the old baseline's smoother, differently-shaped curve — this
  is the LAM-vs-LLI shape signature Finding 1 is about. Note the new
  baseline currently terminates slightly *before* real at both RPTs (a
  separate, already-tracked capacity-fade-timing/magnitude gap, not a new
  problem introduced here — see the `rpt_gap_pp` numbers throughout this
  session's sweep results).
- `final_rpt45_dvdq_3way.png` — dV/dQ for the same three curves. The more
  diagnostic, curvature-sensitive view: the old baseline's dV/dQ shows a
  distinctive double-dip around Q=0.2-0.6 not present in the real curve;
  the new baseline's dV/dQ tracks the real curve's smoother shape more
  closely through that same region.

---

## Finding 2: post-knee LAM comes from porosity-gated, SEI-induced isolation

### Mechanism, in words

Silicon's large volume swing on lithiation/delithiation, combined with SEI
growth on cracked/exposed surface, progressively closes the negative
electrode's pore network. As porosity falls, at some point remaining
electrolyte-filled pathways become so constricted that pockets of active
material lose electronic/ionic access — they become electrically isolated
and stop contributing to charge/discharge, i.e. Loss of Active Material
(LAM). This is modelled as a *gate* — a smooth, porosity-headroom-dependent
function — that ramps a fraction of the ongoing SEI-forming reaction
current from "counts toward SEI growth/LLI" to "counts toward LAM instead",
staying small pre-knee (deliberately, so it doesn't perturb the
already-tuned pre-knee fit) and growing sharply as the electrode's porosity
approaches its floor (post-knee).

### Equations

Implemented in
`src/pybamm/models/submodels/active_material/loss_active_material.py`
(`get_coupled_variables`, active whenever a phase's `"loss of active
material"` option string contains `"porosity"`, e.g. Si's
`"reaction-driven and porosity isolation"`):

**1. Porosity headroom fraction** (1 at as-built porosity, 0 at the floor):

> f_headroom = clamp( (ε_n − ε_n,floor) / (ε_n,0 − ε_n,floor) , 0, 1 )

```
headroom_frac = clip((eps_n - eps_floor) / (eps_n0 - eps_floor), 0, 1)
```
where `eps_n` (ε_n) is the electrode's current (structural) porosity, `eps_n0`
(ε_n,0) its as-set-up (BoL) value, `eps_floor` (ε_n,floor) the parameterised
porosity floor (`"Negative electrode porosity floor"`).

**2. Isolation-gate target** (algebraic, porosity-dependent):

> g_target = (1 − f_headroom) ^ η

```
isolation_gate_target = (1 - headroom_frac) ^ eta_LAM_iso
```
η (`eta_LAM_iso`, `"...porosity-isolation LAM exponent"`) sets the knee's
sharpness/width — a small headroom fraction (near the floor) drives the target
toward 1 (full isolation), large headroom fraction keeps it near 0.

**3. Isolation-gate state** (a lagged ODE state, not an instantaneous
snapshot of the target — added so the gate can't jump discontinuously if
the target itself has a kink):

> dg/dt = (g_target − g) / τ

```
d(isolation_gate)/dt = (isolation_gate_target - isolation_gate) / tau_LAM_iso
```
τ (`tau_LAM_iso`, `"...porosity-isolation gate time constant [s]"`) is the
relaxation time constant (`set_rhs`), and g (`isolation_gate`) the gate
state itself.

**4. Remaining-active-fraction self-limiter** (so the isolation rate
decays toward 0 as the phase is consumed, instead of driving the active
volume fraction through/past zero):

> f_remain = clamp( ε_s / ε_s,0 , 0, 1 )

```
remaining_frac = clip(eps_solid / eps_solid_init, 0, 1)
```
where ε_s (`eps_solid`) is the phase's current active-material volume
fraction and ε_s,0 (`eps_solid_init`) its BoL value.

**5. Isolation-driven active-material loss rate.** Two implementations
exist, selected by `"SEI reaction redirect to LAM"`:

- **Redirect-to-LAM** (the one used for CELL064's Si, current recommended
  form — current-conserving, reuses the exact reaction current
  `sei_growth.py` has already stopped counting toward SEI/LLI):

  > j_iso = γ · g · (a·j_sei) / F · f_remain

  ```
  j_iso = redirect_LAM_yield * isolation_gate * a_j_sei / F * remaining_frac
  ```
  where a·j_sei (`a_j_sei`) is the SEI volumetric interfacial current density
  [A/m^3], F Faraday's constant, γ (`redirect_LAM_yield`,
  `"...SEI-redirect-to-LAM yield"`) the fraction of the redirected current
  that manifests as LAM (rather than vanishing).
- **L_sei-driven** (an alternative form, not the one used for CELL064):

  > j_iso = −β · L_sei · g · f_remain

  ```
  j_iso = -beta_LAM_iso * L_sei * isolation_gate * remaining_frac
  ```
  driven by accumulated SEI thickness L_sei (`L_sei`, a monotonically
  non-decreasing stock) rather than the instantaneous SEI reaction current,
  so it does not collapse to 0 once electrolyte access is diffusion-limited
  — β (`beta_LAM_iso`) is `"...porosity-isolation LAM factor [s-1.m-1]"`.

Either way, j_iso (`j_iso`) is added into the phase's active-material-fraction ODE:

> dε_s/dt = ... + j_iso

```
d(eps_solid)/dt += j_iso
```
(alongside any stress-driven/reaction-driven LAM terms already present),
and the corresponding lithium trapped in the now-isolated material is
tracked as `lli_due_to_lam` (Sulzer et al. 2021 eq. 37's channel) so it
counts toward the model's total LLI, not just LAM.

### How this fits into core PyBaMM's submodel structure

- **Porosity submodel** (`src/pybamm/models/submodels/porosity/reaction_driven_porosity.py`):
  computes structural porosity `eps_n` as a function of accumulated
  SEI/plating thickness — this is what actually closes as the cell ages,
  and what `headroom_frac` above reads.
- **Active-material submodel** (`loss_active_material.py`, above): reads
  `eps_n` (from porosity, built earlier in the submodel call order — see
  build-order note below) and `a_j_sei`/`L_sei` (from the SEI submodel) to
  compute and apply `j_iso`.
- **SEI submodel** (`src/pybamm/models/submodels/interface/sei/sei_growth.py`):
  under the redirect-to-LAM option, reduces its own SEI-film-growth rate
  `dcdt_sei` by the *same* `isolation_gate` fraction, so the redirected
  current is genuinely moved from "SEI growth" to "LAM", not double-counted.
- **Build-order constraint** (discovered/confirmed this session):
  `base_lithium_ion_model.py`'s `set_submodels()` registers submodels in a
  fixed order — `particle mechanics` (crack/expansion) is registered
  *before* `porosity` and `active material`. Within one coupled-variables
  pass, a submodel may only safely read a *coupled* (non-fundamental)
  variable from a submodel registered *earlier* in that list. This means
  `base_mechanics.py` (particle mechanics/expansion) **cannot** read
  `loss_active_material.py`'s `isolation_gate`/`j_iso` — but
  `loss_active_material.py` (registered later) *can* safely read and patch
  mechanics' already-built `"Cell thickness change [m]"` output. This
  ordering constraint governed where several of this session's expansion-
  related mechanisms could physically be implemented.

---

## Finding 3: BoL solid-diffusion parameterization (particle radius & diffusivity) controls the EoL stoichiometric collapse

### The problem, mechanistically

Si's solid-state diffusivity (`silicon_LGM50_diffusivity_Bonkile2024`,
D_ref = 3.0e-16 m^2/s) and particle radius (1.52e-6 m) are both literature
placeholders carried into CELL064's parameter set unfitted — flagged
`[LITERATURE - TO FIT]` in `cell064_parameters.py`'s own provenance notes,
since no high-C-rate pouch data exists to fit them against directly.

As the cell ages, Si loses active material via the porosity-isolation
mechanism (Finding 2) — by EFC~200-250 the model's own Si active-volume
fraction `eps_s` can fall to ~5-10% of its BoL value. The **same total
discharge/charge current is now forced through a much smaller remaining
active-Si volume**, so the *local* current density on the surviving
particles rises sharply — even at a nominally slow C-rate (a "C/20" RPT can
become a locally much higher effective rate once enough Si is isolated).
Combined with Si's small particle radius and low diffusivity, the
solid-diffusion time constant

```
tau_diffusion ~ R^2 / D
```

becomes large relative to the time available for lithium to
equilibrate across the remaining particle at this elevated local current
density. The result is severe solid-diffusion (concentration) polarisation:
the particle surface hits the delithiation/lithiation limit and the
terminal voltage crosses the cutoff **long before the bulk of the particle
has actually been drained/filled** — i.e. the model's own particle-surface
stoichiometry excursion within a cycle collapses to a tiny sliver, even
though the true thermodynamically-accessible capacity is much larger.

Confirmed directly this session: with the OLD (unfitted) diffusivity/radius,
the model's own Si stoichiometric window (`sto_max - sto_min` within one
discharge) collapsed **~9-300x** from early life to EoL, depending on
whether measured on the slow C/20 RPT diagnostic or the faster C/3 ageing
cycle. Real CELL064 data shows nothing like this — see the table below.

### Real experimental stoichiometric ranges (from CELL064's own DMA/eSOH fits)

Extracted directly from Pouch_Data's already-validated
`discharge_esoh_fit.py` output (the raw per-RPT discharge CSVs carry a
`model_x_si`/`model_x_gr` column — the eSOH fit's own reconstructed
stoichiometry at every measured point); `x_si_delta = max(model_x_si) -
min(model_x_si)` over one full discharge:

| RPT | EFC | source | x_si_max | x_si_min | **x_si_delta** | x_gr_delta |
|---|---|---|---|---|---|---|
| 0 | 0.8 | hi-rate neighbor | 0.987 | 0.060 | **0.927** | 0.827 |
| 1 | 50.6 | C/20 RPT | 0.993 | 0.003 | **0.990** | 0.895 |
| 2 | 100.0 | C/20 RPT | 0.984 | 0.003 | **0.981** | 0.846 |
| 3 | 148.5 | hi-rate neighbor | 0.991 | 0.065 | **0.926** | 0.755 |
| **4** | **222.0** | C/20 RPT | 0.990 | 0.211 | **0.780** | 0.783 |
| **5** | **248.0** | C/20 RPT | 0.988 | 0.069 | **0.920** | 0.838 |

Real Si's stoichiometric window barely narrows across the whole real
life — worst case a **1.27x** reduction at RPT4 (near the knee), and it
*recovers* by RPT5. This directly contradicts the old baseline's ~9-300x
collapse: the real cell is nowhere near as diffusion/polarisation-limited
at EoL as the unfitted parameters made the model.

### The fix and its effect

Wired `C64_SI_DIFFUSIVITY_MULT` and `C64_SI_PARTICLE_RADIUS_MULT` (simple
scalar multipliers on the literature values) into
`cell064_degradation_fit.py`'s `PARAM_UPDATES`. Particle radius is the
stronger lever per unit change (`tau ~ R^2/D`, quadratic vs. diffusivity's
linear effect) — a radius cut to x0.2 of the literature value, combined
with a x3 diffusivity increase, was adopted for the new baseline. This
relieves the solid-diffusion bottleneck enough that the remaining active Si
can actually equilibrate across a full discharge even once most of the
phase has been isolated — allowing the model's stoichiometric window to
stay close to fully utilised at EoL, matching the real DMA data's ~1.27x
(not ~9-300x) collapse.

**Plots:** (split into three, per feedback — a single combined figure with
real + old + new, at both C/20 and C/3 rates, got too crowded)
- `dma_si_real_window.png` — real per-RPT Si/Gr stoichiometric window
  (reference, unchanged).
- `dma_si_c20rpt_comparison.png` — the apples-to-apples comparison: real Si
  (C/20) vs. old baseline vs. new baseline, each on their own C/20 RPT legs.
  The new baseline's curve tracks the real curve closely (both stay above
  ~0.9 through EFC~180); the old baseline collapses to ~0.1 by EFC~220.
- `dma_si_c3_comparison.png` — old vs. new baseline's C/3 ageing-leg
  collapse (no real-data equivalent exists at this rate). The new
  baseline's collapse is far more gradual and reaches a much higher floor
  (~0.46 vs. old's ~0.07) by the end of the compared EFC range.
- `stoichiometry_evolution_sidiff_x1.png` (Finding 3's old baseline —
  isolation ON, radius/diffusivity unfitted) vs.
  `stoichiometry_evolution_pbuf2_f0p7_w0p03.png` (new baseline) — the
  standard per-RPT Gr/Si stoichiometry-window plot, direct before/after
  comparison. In the old baseline, the Si panel's latest-RPT curves visibly
  collapse to a thin sliver near full lithiation; in the new baseline they
  retain a full swing down to ~0 even at the last RPT.
- `si_diffusivity_sweep_summary.png` / `all_levers_summary.png` /
  `c3_collapse_vs_fit_landscape.png` — the full lever-sweep landscape
  (diffusivity, exchange-current, particle radius, and combinations) that
  led to this fix, including the levers that did NOT help (K_SEI retune,
  exchange-current-only, and combinations that looked good on some metrics
  but were revealed as truncation artifacts on others).

---

## Files referenced in this write-up

All under `si_gr_expansion_precursor/pouch_cell_064/`:
- `dma/REPORT.md` — the fuller technical log of the diffusivity/exchange-
  current/particle-radius investigation (Findings 3's detailed backing).
- `dma/real_si_gr_sto_window_by_rpt.csv` — the real stoichiometric-range
  table in full.
- `dma/writeup_*.png` — the new comparison plots generated for this
  write-up (RPT4/5 V(Q), dV/dQ, LAM, LLI).
- `degradation_test_matrix/cell064_degradation_fit.py` — the fit driver;
  see its `PARAM_UPDATES` dict and the `SI_DIFFUSIVITY_MULT`/
  `SI_PARTICLE_RADIUS_MULT`/`SI_K_SEI_MULT`/`GR_K_SEI_MULT`/
  `NEG_POROSITY_BOL` definitions for the exact old/new values.
